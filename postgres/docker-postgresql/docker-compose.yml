 version: '2'
 services:

    db:
      build:
        context: .

      restart: always
    #  image: postgresql:9.4
      ports:
        - "5432:5432"
      environment:
        - DEBUG=false

        - DB_USER=postgres
        - DB_PASS=workfree
        - DB_NAME= 'tryton', 'django'
    #    - DB_TEMPLATE=
    #
    #    - DB_EXTENSION=
    #
    #    - REPLICATION_MODE=
    #    - REPLICATION_USER=
    #    - REPLICATION_PASS=
    #    - REPLICATION_SSLMODE=
    #  volumes:
    #    - /srv/docker/postgresql:/var/lib/postgresql
